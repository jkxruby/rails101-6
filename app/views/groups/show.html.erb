<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_group_path(@group), class: "btn btn-primary pull-right")%>
  </div>
  <h1><%= @group.title %></h1>
  <h3><p><%= @group.description %></p></h3>
</div>
